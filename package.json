{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^1.19.4"
  },
  "scripts": {
    "start": "node index.js",
    "build:ui": "rm -rf build && cd ./frontend && yarn run build && cp -r build ../",
    "deploy": "git push -f heroku setup-backend:master",
    "deploy:full": "yarn run build:ui && git add . && git commit -m uibuild && git push && yarn run deploy"
  },
  "engines": {
    "node": "10.14.2"
  }
}