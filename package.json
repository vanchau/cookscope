{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "morgan": "^1.9.1",
    "nodemon": "^1.19.4"
  },
  "scripts": {
    "start": "node server.js",
    "build:ui": "rm -rf build && cd frontend && yarn run build --prod && cp -r build ../",
    "deploy": "git push heroku master",
    "deploy:full": "yarn run build:ui && git add . && git commit -m uibuild && git push && yarn run deploy",
    "logs:prod": "heroku logs --tail"
  }
}
