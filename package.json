{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "nodemon": "^1.19.4"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd frontend && yarn install --only=dev && yarn install && yarn run build"
  }
}